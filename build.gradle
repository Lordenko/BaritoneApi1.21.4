plugins {
    id 'java'
    id 'maven-publish'
}

group = 'com.github.Lordenko'  // ⚠️ Заміни на GitHub username
version = '1.0'

repositories {
    mavenCentral()
}

dependencies {
    // нічого, бо ми просто обгортаємо .jar
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            artifactId = 'baritone-fabric'
            from components.java
            artifact file('baritone-api-fabric-1.11.1-3-g026517a9.jar')
        }
    }
}
